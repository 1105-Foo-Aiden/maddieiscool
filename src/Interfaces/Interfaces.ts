export interface IToken {
    token: string
}

export interface IUserData {
    username: string,
    password: string
}

export interface ItemType {
    id: number;
    title: string;
    desc: string;
    priority: string;
    userPFP: string;
}